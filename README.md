# Action Bot — Система Модерации и Анти-нюк

Мощный Discord-бот для модерации и защиты сервера, написанный на TypeScript с использованием Discord.js v14 и SQLite.

## ✨ Особенности

- **Единая панель управления (`/settings`)**: Настройка всех параметров бота через удобное интерактивное меню с модальными окнами.
- **Команда модерации (`/action`)**: Быстрое управление пользователем (бан, варн, мут, разбан и т.д.) через графический интерфейс.
- **Локальная База Данных**: Полная независимость от внешних API. Все настройки и история наказаний хранятся локально в `punishments.db`.
- **Гибкая система прав**:
    - Доступ к командам только для Администраторов по умолчанию.
    - Поддержка "Белого списка" (Whitelist) для отдельных ролей и пользователей.
- **Автоматизация**:
    - Система предупреждений (варнов) с автоматическим баном при достижении лимита.
    - Срок действия варнов (7 дней).
    - Автоматическое удаление уведомлений об успехе через 60 секунд.
    - Отключение кнопок меню при неактивности.
- **Логирование**: Подробные логи всех действий модераторов в указанный канал.

## 🚀 Установка и запуск

### Предварительные требования
- Node.js (v16.11.0 или выше)
- npm

### 1. Клонирование репозитория
```bash
git clone https://github.com/scronbrand/Action.git
cd Action
```

### 2. Установка зависимостей
```bash
npm install
```

### 3. Настройка переменных окружения
Создайте файл `.env` в корневой директории и добавьте ваш токен бота:
```env
DISCORD_TOKEN=ваш_токен_бота
```

### 4. Компиляция проекта
```bash
npm run build
```

### 5. Запуск бота
```bash
npm start
```

## 🛠 Команды

| Команда | Описание |
| :--- | :--- |
| `/action [пользователь]` | Открывает меню действий для указанного пользователя (Бан, Варн, Мут и т.д.). |
| `/settings` | Открывает панель управления настройками бота (роли, логи, лимиты, вайтлист). |

## ⚙️ Настройка (Dashboard)
После запуска бота используйте `/settings`, чтобы настроить:
1. **Роль карантина**: Роль, которая выдается при бане.
2. **Роль участника**: Роль обычного пользователя (используется при разбане).
3. **Канал уведомлений**: Канал, куда будут отправляться логи действий.
4. **Лимит варнов**: Количество предупреждений до авто-бана.
5. **Белый список**: Роли или пользователи, которым разрешено использовать бота без прав администратора.

---
Разработано с ❤️ командой Google Deepmind.
